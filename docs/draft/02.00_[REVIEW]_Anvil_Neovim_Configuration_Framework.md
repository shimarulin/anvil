# Anvil — Unified Neovim Configuration Framework

## Архитектура

```
anvil/
├── src/
│   ├── common/                    # Общая конфигурация (anvil core)
│   │   ├── lua/
│   │   │   └── anvil/
│   │   │       ├── init.lua       # Точка входа общей конфигурации
│   │   │       ├── options.lua    # Общие vim options
│   │   │       ├── keymaps.lua    # Общие маппинги
│   │   │       ├── autocmds.lua   # Общие автокоманды
│   │   │       ├── utils.lua      # Утилиты
│   │   │       └── plugins/       # Общие спеки плагинов (lazy.nvim формат)
│   │   │           ├── editor.lua
│   │   │           ├── ui.lua
│   │   │           ├── git.lua
│   │   │           └── lang.lua
│   │   └── after/                 # Общие ftplugin и т.д.
│   │       └── ftplugin/
│   │           ├── python.lua
│   │           ├── lua.lua
│   │           └── markdown.lua
│   │
│   ├── astronvim/                 # AstroNvim starter template + anvil интеграция
│   │   ├── init.lua
│   │   └── lua/
│   │       ├── lazy_setup.lua
│   │       ├── community.lua
│   │       ├── polish.lua
│   │       └── plugins/
│   │           ├── astrocore.lua
│   │           ├── astrolsp.lua
│   │           ├── astroui.lua
│   │           ├── mason.lua
│   │           ├── none-ls.lua
│   │           ├── treesitter.lua
│   │           ├── user.lua
│   │           └── anvil_bridge.lua  # Мост к common
│   │
│   ├── nvchad/                    # NvChad starter template + anvil интеграция
│   │   ├── init.lua
│   │   └── lua/
│   │       ├── chadrc.lua
│   │       ├── options.lua
│   │       ├── mappings.lua
│   │       ├── autocmds.lua
│   │       ├── configs/
│   │       │   ├── conform.lua
│   │       │   ├── lazy.lua
│   │       │   └── lspconfig.lua
│   │       ├── plugins/
│   │       │   └── init.lua
│   │       └── anvil_bridge.lua   # Мост к common
│   │
│   └── standalone/                # Минимальная конфигурация без фреймворка
│       ├── init.lua
│       └── lua/
│           └── ...
│
├── preinstall.sh          # Скрипт установки системных зависимостей
├── install.sh             # Скрипт установки/управления конфигурациями anvil
├── Makefile               # Альтернативный интерфейс
├── README.md
└── .gitignore
```

## Ключевой механизм — Common Bridge

Основная идея: `src/common` не является самостоятельной neovim-конфигурацией.
Это Lua-библиотека, которую каждый фреймворк подключает через добавление пути
в `runtimepath` и затем вызывает нужные модули, адаптируя их к своему формату
конфигурации.

### Принцип совместимости с upstream templates

**Критически важно:** файлы фреймворков (astronvim, nvchad) должны оставаться
максимально близкими к оригинальным starter templates. Anvil интеграция
добавляется **аддитивно**:

- `init.lua` — сохраняется оригинальная логика bootstrap, добавляется только
  вызов bridge
- `lazy_setup.lua` — сохраняются все оригинальные imports (`community`, etc.)
- Существующие plugin specs — остаются с оригинальными guard-строками
  (`if true then return {} end`), активируются пользователем по необходимости
- `anvil_bridge.lua` — единственный новый файл, всё остальное — минимальные
  правки существующих

---

## src/common — Общая библиотека

### src/common/lua/anvil/init.lua

```lua
-- Anvil Common Configuration Core
-- Это библиотека, а не самостоятельная конфигурация.
-- Подключается через runtimepath из astronvim или nvchad.

local M = {}

M._loaded = false

--- Определяет, в каком фреймворке работаем
---@return "astronvim"|"nvchad"|"standalone"|"unknown"
function M.detect_framework()
  -- AstroNvim определяется по наличию модуля astrocore
  local has_astro = pcall(require, "astrocore")
  if has_astro then return "astronvim" end

  -- NvChad определяется по наличию модуля nvconfig или chadrc
  local has_nvchad = pcall(require, "nvconfig")
  if has_nvchad then return "nvchad" end

  -- Проверяем глобальную переменную, которую может выставить standalone
  if vim.g.anvil_framework then
    return vim.g.anvil_framework
  end

  return "unknown"
end

--- Основная точка входа. Вызывается из bridge-модуля конкретного фреймворка.
---@param opts? table
function M.setup(opts)
  if M._loaded then return end
  M._loaded = true

  opts = opts or {}

  -- Применяем общие vim options
  -- NB: для NvChad вызываем ПОСЛЕ require("nvchad.options"),
  --     для AstroNvim — опции задаются через astrocore opts,
  --     поэтому по умолчанию options не применяются напрямую.
  if opts.apply_options then
    require("anvil.options").setup(opts.options or {})
  end

  -- Применяем общие автокоманды
  if opts.apply_autocmds ~= false then
    require("anvil.autocmds").setup(opts.autocmds or {})
  end

  -- Маппинги: по умолчанию НЕ применяем напрямую.
  -- AstroNvim использует astrocore mappings API.
  -- NvChad использует свой mappings.lua + nvchad.mappings.
  -- Bridge каждого фреймворка решает, как применить.
  if opts.apply_keymaps then
    require("anvil.keymaps").setup(opts.keymaps or {})
  end
end

--- Возвращает общие спеки плагинов в формате lazy.nvim
--- Оба фреймворка используют lazy.nvim, поэтому формат единый
---@param opts? table Переопределения и фильтры
---@return table[] Массив lazy.nvim plugin specs
function M.plugin_specs(opts)
  opts = opts or {}
  local specs = {}

  local spec_modules = {
    "anvil.plugins.editor",
    "anvil.plugins.ui",
    "anvil.plugins.git",
    "anvil.plugins.lang",
  }

  -- Позволяем отключить определённые группы
  local disabled = opts.disabled_groups or {}
  local disabled_set = {}
  for _, g in ipairs(disabled) do
    disabled_set[g] = true
  end

  for _, mod_name in ipairs(spec_modules) do
    local group = mod_name:match("anvil%.plugins%.(.+)")
    if not disabled_set[group] then
      local ok, mod = pcall(require, mod_name)
      if ok then
        if type(mod) == "table" then
          -- Модуль может вернуть один спек или массив спеков
          if mod[1] or mod.dir or mod.url then
            -- Одиночный спек (имеет [1] как short url или dir/url)
            table.insert(specs, mod)
          else
            -- Таблица спеков (ключи — числовые)
            for _, spec in ipairs(mod) do
              table.insert(specs, spec)
            end
          end
        end
      end
    end
  end

  return specs
end

--- Возвращает общие маппинги в нормализованном формате
---@return table[]
function M.get_keymaps()
  return require("anvil.keymaps").get_all()
end

--- Возвращает общие опции для LSP
---@return table
function M.lsp_opts()
  return require("anvil.plugins.lang").lsp_opts()
end

return M
```

### src/common/lua/anvil/options.lua

```lua
local M = {}

-- Общие опции, не зависящие от фреймворка.
-- Эти значения совпадают с тем, что AstroNvim и NvChad ставят по умолчанию,
-- но служат единым источником правды для standalone и для переопределений.
local default_options = {
  -- Визуальные
  number = true,
  relativenumber = true,
  signcolumn = "yes",
  cursorline = true,
  termguicolors = true,
  showmode = false,
  wrap = false,
  linebreak = true,

  -- Отступы
  expandtab = true,
  shiftwidth = 2,
  tabstop = 2,
  softtabstop = 2,
  smartindent = true,

  -- Поиск
  ignorecase = true,
  smartcase = true,
  hlsearch = true,
  incsearch = true,

  -- Поведение
  splitbelow = true,
  splitright = true,
  scrolloff = 8,
  sidescrolloff = 8,
  mouse = "a",
  clipboard = "unnamedplus",
  undofile = true,
  updatetime = 250,
  timeoutlen = 300,

  -- Completion
  completeopt = "menu,menuone,noselect",
}

--- Возвращает таблицу опций (для фреймворков, которые применяют через свой API)
---@param overrides? table
---@return table
function M.get(overrides)
  return vim.tbl_deep_extend("force", default_options, overrides or {})
end

--- Применяет опции напрямую через vim.opt
--- Используется в standalone конфигурации.
--- Для AstroNvim опции передаются через astrocore opts.
--- Для NvChad — через options.lua после require("nvchad.options").
---@param overrides? table
function M.setup(overrides)
  local opts = M.get(overrides)

  for k, v in pairs(opts) do
    if type(k) == "string" then
      local ok = pcall(function() vim.opt[k] = v end)
      if not ok then
        vim.notify(
          string.format("[anvil] Failed to set option: %s = %s", k, vim.inspect(v)),
          vim.log.levels.WARN
        )
      end
    end
  end
end

return M
```

### src/common/lua/anvil/keymaps.lua

```lua
local M = {}

-- Формат: { mode, lhs, rhs, opts }
-- Это общие маппинги, которые имеют смысл в любом фреймворке.
-- Фреймворко-специфичные маппинги (buffer navigation через astrocore.buffer,
-- NvChad telescope mappings и т.д.) остаются в соответствующих конфигурациях.
local keymaps = {
  -- ── Resize окон ──
  { "n", "<C-Up>", "<Cmd>resize -2<CR>", { desc = "Resize split up" } },
  { "n", "<C-Down>", "<Cmd>resize +2<CR>", { desc = "Resize split down" } },
  { "n", "<C-Left>", "<Cmd>vertical resize -2<CR>", { desc = "Resize split left" } },
  { "n", "<C-Right>", "<Cmd>vertical resize +2<CR>", { desc = "Resize split right" } },

  -- ── Перемещение строк ──
  { "n", "<A-j>", "<Cmd>m .+1<CR>==", { desc = "Move line down" } },
  { "n", "<A-k>", "<Cmd>m .-2<CR>==", { desc = "Move line up" } },
  { "v", "<A-j>", ":m '>+1<CR>gv=gv", { desc = "Move selection down" } },
  { "v", "<A-k>", ":m '<-2<CR>gv=gv", { desc = "Move selection up" } },

  -- ── Общие удобства ──
  { "n", "<Esc>", "<Cmd>nohlsearch<CR>", { desc = "Clear search highlight" } },
  { "n", "<Leader>w", "<Cmd>w<CR>", { desc = "Save file" } },
  { "n", "<Leader>q", "<Cmd>q<CR>", { desc = "Quit" } },

  -- ── Навигация по окнам ──
  { "n", "<C-h>", "<C-w>h", { desc = "Move to left window" } },
  { "n", "<C-j>", "<C-w>j", { desc = "Move to lower window" } },
  { "n", "<C-k>", "<C-w>k", { desc = "Move to upper window" } },
  { "n", "<C-l>", "<C-w>l", { desc = "Move to right window" } },

  -- ── Диагностика (базовые, фреймворк может переопределить) ──
  { "n", "]d", function() vim.diagnostic.goto_next() end, { desc = "Next diagnostic" } },
  { "n", "[d", function() vim.diagnostic.goto_prev() end, { desc = "Previous diagnostic" } },
}

--- Применяет маппинги напрямую через vim.keymap.set
---@param overrides? table Дополнительные маппинги в том же формате
function M.setup(overrides)
  overrides = overrides or {}

  for _, map in ipairs(keymaps) do
    local mode, lhs, rhs, opts = map[1], map[2], map[3], map[4] or {}
    opts = vim.tbl_extend("force", { silent = true, noremap = true }, opts)
    vim.keymap.set(mode, lhs, rhs, opts)
  end

  -- Применяем пользовательские переопределения
  for _, map in ipairs(overrides) do
    local mode, lhs, rhs, opts = map[1], map[2], map[3], map[4] or {}
    opts = vim.tbl_extend("force", { silent = true, noremap = true }, opts)
    vim.keymap.set(mode, lhs, rhs, opts)
  end
end

--- Возвращает все маппинги в структурированном виде
---@return table[]
function M.get_all()
  return keymaps
end

--- Конвертирует маппинги в формат AstroCore mappings
--- AstroCore формат: { n = { ["<lhs>"] = { <rhs>, desc = "..." } } }
---@return table
function M.to_astrocore_format()
  local result = {}
  for _, map in ipairs(keymaps) do
    local mode, lhs, rhs, opts = map[1], map[2], map[3], map[4] or {}
    if not result[mode] then result[mode] = {} end
    result[mode][lhs] = { rhs, desc = opts.desc or "" }
  end
  return result
end

--- Конвертирует маппинги в формат NvChad mappings
---@return table
function M.to_nvchad_format()
  local result = {}
  for _, map in ipairs(keymaps) do
    local mode, lhs, rhs, opts = map[1], map[2], map[3], map[4] or {}
    if not result[mode] then result[mode] = {} end
    result[mode][lhs] = { rhs, opts.desc or "" }
  end
  return result
end

return M
```

### src/common/lua/anvil/autocmds.lua

```lua
local M = {}

---@param overrides? table
function M.setup(overrides)
  overrides = overrides or {}

  local augroup = vim.api.nvim_create_augroup("AnvilCommon", { clear = true })

  -- Подсветка при yank
  vim.api.nvim_create_autocmd("TextYankPost", {
    group = augroup,
    desc = "Highlight on yank",
    callback = function()
      vim.highlight.on_yank({ higroup = "IncSearch", timeout = 200 })
    end,
  })

  -- Убираем trailing whitespace при сохранении
  vim.api.nvim_create_autocmd("BufWritePre", {
    group = augroup,
    desc = "Remove trailing whitespace",
    pattern = "*",
    callback = function()
      if vim.bo.filetype == "diff" then return end
      local save = vim.fn.winsaveview()
      vim.cmd([[%s/\s\+$//e]])
      vim.fn.winrestview(save)
    end,
  })

  -- Автоматически возвращаемся к последней позиции в файле
  vim.api.nvim_create_autocmd("BufReadPost", {
    group = augroup,
    desc = "Restore cursor position",
    callback = function(args)
      local mark = vim.api.nvim_buf_get_mark(args.buf, '"')
      local line_count = vim.api.nvim_buf_line_count(args.buf)
      if mark[1] > 0 and mark[1] <= line_count then
        pcall(vim.api.nvim_win_set_cursor, 0, mark)
      end
    end,
  })

  -- Автоматический resize при изменении размера терминала
  vim.api.nvim_create_autocmd("VimResized", {
    group = augroup,
    desc = "Auto resize panes",
    callback = function()
      vim.cmd("tabdo wincmd =")
    end,
  })

  -- Пользовательские автокоманды
  for _, ac in ipairs(overrides) do
    vim.api.nvim_create_autocmd(ac.event, vim.tbl_extend("force", {
      group = augroup,
    }, ac.opts or {}))
  end
end

return M
```

### src/common/lua/anvil/utils.lua

```lua
local M = {}

--- Проверяет, установлен ли исполняемый файл
---@param name string
---@return boolean
function M.is_executable(name)
  return vim.fn.executable(name) == 1
end

--- Безопасный require с fallback
---@param mod string
---@return any|nil
function M.safe_require(mod)
  local ok, result = pcall(require, mod)
  if ok then return result end
  return nil
end

--- Путь к директории common (определяется автоматически)
---@return string
function M.common_path()
  -- Ищем путь к текущему файлу и поднимаемся до common/
  local source = debug.getinfo(1, "S").source:sub(2) -- убираем @
  -- source = .../src/common/lua/anvil/utils.lua
  local common_lua = vim.fn.fnamemodify(source, ":h:h") -- .../src/common/lua
  local common = vim.fn.fnamemodify(common_lua, ":h")    -- .../src/common
  return common
end

--- Добавляет common в runtimepath если ещё не добавлен
function M.ensure_rtp()
  local common = M.common_path()
  if not vim.tbl_contains(vim.opt.runtimepath:get(), common) then
    vim.opt.runtimepath:prepend(common)
  end
end

--- Мержит несколько таблиц lazy.nvim спеков
---@param ... table[] Массивы спеков
---@return table[] Объединённый массив
function M.merge_specs(...)
  local result = {}
  for _, specs in ipairs({ ... }) do
    for _, spec in ipairs(specs) do
      table.insert(result, spec)
    end
  end
  return result
end

return M
```

### src/common/lua/anvil/plugins/editor.lua

```lua
-- Общие плагины для редактирования, которые не конфликтуют
-- ни с AstroNvim, ни с NvChad.
--
-- NB: nvim-autopairs уже включён в оба фреймворка по умолчанию,
-- поэтому здесь НЕ добавляем его (иначе будет конфликт спеков).
-- Возвращаем массив lazy.nvim спеков.

return {
  -- Surround (работает в обоих фреймворках)
  {
    "kylechui/nvim-surround",
    version = "*",
    event = "VeryLazy",
    opts = {},
  },

  -- Подсветка TODO/FIXME/HACK и т.д.
  {
    "folke/todo-comments.nvim",
    event = "BufReadPost",
    dependencies = { "nvim-lua/plenary.nvim" },
    opts = {},
  },

  -- Улучшенный поиск и замена
  {
    "nvim-pack/nvim-spectre",
    cmd = "Spectre",
    opts = {},
  },
}
```

### src/common/lua/anvil/plugins/ui.lua

```lua
-- Общие UI плагины
-- Пока пустой — UI сильно зависит от фреймворка.
-- Сюда можно добавлять плагины, которые не конфликтуют
-- с AstroNvim heirline/astroui и NvChad base46/nvchad-ui.

return {}
```

### src/common/lua/anvil/plugins/git.lua

```lua
-- Общие git плагины
-- NB: gitsigns уже включён в оба фреймворка, не дублируем.
-- Сюда можно добавить дополнительные git-инструменты.

return {}
```

### src/common/lua/anvil/plugins/lang.lua

```lua
-- Общие языковые настройки
-- Этот модуль НЕ возвращает lazy.nvim спеки напрямую,
-- а предоставляет данные, которые bridge-модули используют
-- для настройки mason, treesitter и lspconfig через API фреймворка.

local M = {}

-- LSP серверы (имена для lspconfig)
M.lsp_servers = {
  "lua_ls",
  "pyright",
  "ts_ls",
  "jsonls",
  "yamlls",
  "bashls",
}

-- Mason-пакеты (имена для mason / mason-tool-installer)
M.mason_tools = {
  "lua-language-server",
  "pyright",
  "typescript-language-server",
  "json-lsp",
  "yaml-language-server",
  "bash-language-server",
  "stylua",
  "prettier",
  "black",
  "isort",
  "shellcheck",
  "shfmt",
}

-- Treesitter парсеры
M.treesitter_ensure_installed = {
  "lua",
  "python",
  "typescript",
  "javascript",
  "json",
  "yaml",
  "bash",
  "html",
  "css",
  "markdown",
  "markdown_inline",
  "vim",
  "vimdoc",
  "regex",
  "toml",
}

---@return table
function M.lsp_opts()
  return {
    servers = M.lsp_servers,
    mason_tools = M.mason_tools,
    treesitter = M.treesitter_ensure_installed,
  }
end

-- Как lazy.nvim спеки — пустой, т.к. LSP настраивается через фреймворк
return {}
```

---

## AstroNvim интеграция

Принцип: **минимальные изменения к оригинальному starter template**.
Единственный новый файл — `anvil_bridge.lua`. Остальные файлы модифицируются
точечно.

### src/astronvim/lua/plugins/anvil_bridge.lua

Новый файл — мост к common.

```lua
-- Мост между AstroNvim и anvil common
-- Этот файл подключает common runtimepath и загружает общую конфигурацию.
--
-- Структура путей:
--   src/astronvim/lua/plugins/anvil_bridge.lua  (этот файл)
--   src/common/                                  (общая библиотека)

local this_file = debug.getinfo(1, "S").source:sub(2)
local plugins_dir = vim.fn.fnamemodify(this_file, ":h")     -- .../lua/plugins
local lua_dir = vim.fn.fnamemodify(plugins_dir, ":h")       -- .../lua
local astronvim_dir = vim.fn.fnamemodify(lua_dir, ":h")     -- .../astronvim
local src_dir = vim.fn.fnamemodify(astronvim_dir, ":h")     -- .../src
local common_dir = src_dir .. "/common"

-- Добавляем common в runtimepath
vim.opt.runtimepath:prepend(common_dir)

-- Загружаем anvil core
-- Для AstroNvim: options применяются через astrocore opts,
-- keymaps — через astrocore mappings, autocmds — можно применить напрямую.
local anvil = require("anvil")
anvil.setup({
  apply_options = false,  -- Опции через astrocore
  apply_keymaps = false,  -- Маппинги через astrocore
  apply_autocmds = true,  -- Автокоманды можно применить напрямую
})

-- Получаем общие плагин-спеки и возвращаем как lazy.nvim плагины
return anvil.plugin_specs()
```

### src/astronvim/lua/plugins/astrocore.lua

Модифицированный файл — убран guard `if true then return {} end`,
добавлена интеграция с anvil keymaps. Сохранена оригинальная структура.

```lua
-- AstroCore provides a central place to modify mappings, vim options,
-- autocommands, and more!
-- Configuration documentation can be found with :h astrocore

-- Подгружаем common keymaps (rtp уже настроен через anvil_bridge)
local ok, anvil_keymaps = pcall(require, "anvil.keymaps")
local common_mappings = {}
if ok then
  common_mappings = anvil_keymaps.to_astrocore_format()
end

---@type LazySpec
return {
  "AstroNvim/astrocore",
  ---@type AstroCoreOpts
  opts = {
    -- Configure core features of AstroNvim
    features = {
      large_buf = { size = 1024 * 256, lines = 10000 },
      autopairs = true,
      cmp = true,
      diagnostics = { virtual_text = true, virtual_lines = false },
      highlighturl = true,
      notifications = true,
    },
    -- Diagnostics configuration (for vim.diagnostics.config({...}))
    diagnostics = {
      virtual_text = true,
      underline = true,
    },
    -- vim options can be configured here
    options = {
      opt = { -- vim.opt.<key>
        relativenumber = true,
        number = true,
        spell = false,
        signcolumn = "yes",
        wrap = false,
        scrolloff = 8,
        sidescrolloff = 8,
      },
      g = { -- vim.g.<key>
        -- configure global vim variables (vim.g)
      },
    },
    -- Маппинги: мержим common + astro-специфичные
    mappings = vim.tbl_deep_extend("force", common_mappings, {
      n = {
        -- AstroNvim-специфичные маппинги (buffer navigation через astrocore)
        ["]b"] = {
          function() require("astrocore.buffer").nav(vim.v.count1) end,
          desc = "Next buffer",
        },
        ["[b"] = {
          function() require("astrocore.buffer").nav(-vim.v.count1) end,
          desc = "Previous buffer",
        },
        ["<Leader>bd"] = {
          function()
            require("astroui.status.heirline").buffer_picker(
              function(bufnr) require("astrocore.buffer").close(bufnr) end
            )
          end,
          desc = "Close buffer from tabline",
        },

        -- Anvil info
        ["<Leader>n"] = { desc = "  Anvil" },
        ["<Leader>ni"] = {
          function()
            vim.notify("Anvil on AstroNvim", vim.log.levels.INFO)
          end,
          desc = "Anvil info",
        },
      },
    }),
  },
}
```

### src/astronvim/lua/plugins/mason.lua

Модифицированный файл — убран guard, добавлена интеграция с anvil lang.

```lua
-- Customize Mason with anvil common tools

local tools = {}
local ok, anvil_lang = pcall(require, "anvil.plugins.lang")
if ok then
  tools = anvil_lang.mason_tools or {}
end

---@type LazySpec
return {
  -- use mason-tool-installer for automatically installing Mason packages
  {
    "WhoIsSethDaniel/mason-tool-installer.nvim",
    opts = {
      -- Make sure to use the names found in :Mason
      ensure_installed = tools,
    },
  },
}
```

### src/astronvim/lua/plugins/treesitter.lua

Модифицированный файл — убран guard, добавлена интеграция с anvil lang.

```lua
-- Customize Treesitter with anvil common parsers

local parsers = {}
local ok, anvil_lang = pcall(require, "anvil.plugins.lang")
if ok then
  parsers = anvil_lang.treesitter_ensure_installed or {}
end

---@type LazySpec
return {
  "nvim-treesitter/nvim-treesitter",
  opts = {
    ensure_installed = parsers,
  },
}
```

### src/astronvim/init.lua

**Без изменений** — оригинальный файл из codebase.md полностью сохраняется.
Bootstrap lazy.nvim и вызов `require "lazy_setup"` / `require "polish"` остаются
как есть. Anvil подключается через `anvil_bridge.lua` в plugins/.

### src/astronvim/lua/lazy_setup.lua

**Без изменений** — оригинальный файл сохраняется полностью, включая
`{ import = "community" }` и все опции.

---

## NvChad интеграция

Принцип тот же: минимальные изменения к оригинальному starter template.

### src/nvchad/lua/anvil_bridge.lua

Новый файл — мост к common.

```lua
-- Мост между NvChad и anvil common
--
-- Структура путей:
--   src/nvchad/lua/anvil_bridge.lua  (этот файл)
--   src/common/                       (общая библиотека)

local M = {}

local this_file = debug.getinfo(1, "S").source:sub(2)
local lua_dir = vim.fn.fnamemodify(this_file, ":h")      -- .../lua
local nvchad_dir = vim.fn.fnamemodify(lua_dir, ":h")     -- .../nvchad
local src_dir = vim.fn.fnamemodify(nvchad_dir, ":h")     -- .../src
local common_dir = src_dir .. "/common"

function M.setup()
  -- Добавляем common в runtimepath
  vim.opt.runtimepath:prepend(common_dir)

  -- Загружаем anvil core
  -- Для NvChad: options применяются в options.lua после nvchad.options,
  -- keymaps — в mappings.lua после nvchad.mappings,
  -- autocmds — можно применить напрямую.
  local anvil = require("anvil")
  anvil.setup({
    apply_options = false,   -- Применим в options.lua
    apply_keymaps = false,   -- Применим в mappings.lua
    apply_autocmds = true,   -- Автокоманды напрямую
  })
end

--- Возвращает общие плагин-спеки для добавления в NvChad plugins
---@return table[]
function M.plugin_specs()
  local anvil = require("anvil")
  return anvil.plugin_specs()
end

--- Возвращает mason tools list
---@return string[]
function M.mason_tools()
  local ok, lang = pcall(require, "anvil.plugins.lang")
  if ok then return lang.mason_tools or {} end
  return {}
end

--- Возвращает treesitter parsers list
---@return string[]
function M.treesitter_parsers()
  local ok, lang = pcall(require, "anvil.plugins.lang")
  if ok then return lang.treesitter_ensure_installed or {} end
  return {}
end

return M
```

### src/nvchad/init.lua

Минимальное изменение — добавлен вызов bridge до загрузки NvChad.
Путь `base46_cache` сохранён как в оригинале (`/base46/`).

```lua
vim.g.base46_cache = vim.fn.stdpath "data" .. "/base46/"
vim.g.mapleader = " "

-- Подключаем anvil bridge до загрузки NvChad
require("anvil_bridge").setup()

-- bootstrap lazy and all plugins
local lazypath = vim.fn.stdpath "data" .. "/lazy/lazy.nvim"

if not vim.uv.fs_stat(lazypath) then
  local repo = "https://github.com/folke/lazy.nvim.git"
  vim.fn.system { "git", "clone", "--filter=blob:none", repo, "--branch=stable", lazypath }
end

vim.opt.rtp:prepend(lazypath)

local lazy_config = require "configs.lazy"

-- load plugins
require("lazy").setup({
  {
    "NvChad/NvChad",
    lazy = false,
    branch = "v2.5",
    import = "nvchad.plugins",
  },

  { import = "plugins" },
}, lazy_config)

-- load theme
dofile(vim.g.base46_cache .. "defaults")
dofile(vim.g.base46_cache .. "statusline")

require "options"
require "autocmds"

vim.schedule(function()
  require "mappings"
end)
```

### src/nvchad/lua/options.lua

Модифицированный файл — после nvchad.options применяем anvil options.

```lua
require "nvchad.options"

-- Применяем общие anvil опции поверх nvchad defaults
local ok, anvil_options = pcall(require, "anvil.options")
if ok then
  anvil_options.setup()
end

-- add yours here!
-- local o = vim.o
-- o.cursorlineopt = 'both' -- to enable cursorline!
```

### src/nvchad/lua/mappings.lua

Модифицированный файл — после nvchad.mappings применяем anvil keymaps.

```lua
require "nvchad.mappings"

-- Применяем общие anvil маппинги
local ok, anvil_keymaps = pcall(require, "anvil.keymaps")
if ok then
  anvil_keymaps.setup()
end

-- add yours here
local map = vim.keymap.set

map("n", ";", ":", { desc = "CMD enter command mode" })
map("i", "jk", "<ESC>")

-- map({ "n", "i", "v" }, "<C-s>", "<cmd> w <cr>")
```

### src/nvchad/lua/plugins/init.lua

Модифицированный файл — добавлены common спеки и treesitter parsers.
Сохранены оригинальные плагины (conform, lspconfig).

```lua
-- Подгружаем anvil common спеки
local bridge_ok, bridge = pcall(require, "anvil_bridge")
local common_specs = bridge_ok and bridge.plugin_specs() or {}
local treesitter_parsers = bridge_ok and bridge.treesitter_parsers() or {}

-- NvChad-специфичные плагины (оригинальные + anvil расширения)
local nvchad_specs = {
  {
    "stevearc/conform.nvim",
    -- event = 'BufWritePre', -- uncomment for format on save
    opts = require "configs.conform",
  },

  -- These are some examples, uncomment them if you want to see them work!
  {
    "neovim/nvim-lspconfig",
    config = function()
      require "configs.lspconfig"
    end,
  },

  -- Treesitter с общими парсерами из anvil
  {
    "nvim-treesitter/nvim-treesitter",
    opts = {
      ensure_installed = treesitter_parsers,
    },
  },
}

-- Мержим common и nvchad спеки
local all_specs = {}
for _, spec in ipairs(common_specs) do
  table.insert(all_specs, spec)
end
for _, spec in ipairs(nvchad_specs) do
  table.insert(all_specs, spec)
end

return all_specs
```

---

## Скрипт установки

### install.sh

```bash
#!/usr/bin/env bash
set -euo pipefail

# ─── Цвета ───
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color

# ─── Определяем директорию скрипта (корень репозитория) ───
SCRIPT_DIR="$$(cd "$$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SRC_DIR="${SCRIPT_DIR}/src"

# ─── Платформо-зависимые пути ───
case "$(uname -s)" in
  Linux|Darwin)
    NVIM_CONFIG_HOME="$${XDG_CONFIG_HOME:-$$HOME/.config}"
    NVIM_DATA_HOME="$${XDG_DATA_HOME:-$$HOME/.local/share}"
    NVIM_STATE_HOME="$${XDG_STATE_HOME:-$$HOME/.local/state}"
    NVIM_CACHE_HOME="$${XDG_CACHE_HOME:-$$HOME/.cache}"
    ;;
  MINGW*|MSYS*|CYGWIN*)
    NVIM_CONFIG_HOME="${LOCALAPPDATA}"
    NVIM_DATA_HOME="${LOCALAPPDATA}"
    NVIM_STATE_HOME="${LOCALAPPDATA}"
    NVIM_CACHE_HOME="${TEMP:-/tmp}"
    ;;
  *)
    echo -e "$${RED}Unsupported platform: $$(uname -s)${NC}"
    exit 1
    ;;
esac

# ─── Функции ───
print_header() {
  echo -e "${CYAN}"
  echo "  ╔═══════════════════════════════════════╗"
  echo "  ║           ⚒  ANVIL  ⚒                ║"
  echo "  ║   Unified Neovim Configuration        ║"
  echo "  ╚═══════════════════════════════════════╝"
  echo -e "${NC}"
}

log_info() { echo -e "$${BLUE}[INFO]$${NC} $*"; }
log_ok()   { echo -e "$${GREEN}[OK]$${NC} $*"; }
log_warn() { echo -e "$${YELLOW}[WARN]$${NC} $*"; }
log_err()  { echo -e "$${RED}[ERROR]$${NC} $*"; }

# Проверяет зависимости
check_deps() {
  local missing=()

  command -v git &>/dev/null  || missing+=("git")
  command -v nvim &>/dev/null || missing+=("nvim")

  if [[ ${#missing[@]} -gt 0 ]]; then
    log_err "Missing required dependencies: ${missing[*]}"
    log_info "Run ./preinstall.sh first to install system dependencies"
    exit 1
  fi

  # Проверяем версию neovim
  local nvim_version
  nvim_version=$(nvim --version | head -1 | grep -oP 'v\K[0-9]+\.[0-9]+' || true)

  if [[ -z "$nvim_version" ]]; then
    log_warn "Could not detect Neovim version"
  else
    local major minor
    major=$$(echo "$$nvim_version" | cut -d. -f1)
    minor=$$(echo "$$nvim_version" | cut -d. -f2)

    if [[ $$major -lt 1 && $$minor -lt 10 ]]; then
      log_err "Neovim v0.10+ required (found v${nvim_version})"
      log_info "Run ./preinstall.sh to install a newer version"
      exit 1
    fi
  fi

  log_ok "Dependencies OK"
}

# Создаёт симлинк с проверками
create_symlink() {
  local source="\$1"
  local target="\$2"

  if [[ -L "$target" ]]; then
    local current_target
    current_target=$$(readlink -f "$$target" 2>/dev/null || readlink "$target")
    local resolved_source
    resolved_source=$$(readlink -f "$$source" 2>/dev/null || readlink "$source")

    if [[ "$$current_target" == "$$resolved_source" ]]; then
      log_ok "Symlink already correct: $$target -> $$source"
      return 0
    else
      log_warn "Symlink exists but points elsewhere: $$target -> $$current_target"
      log_info "Updating to: $$target -> $$source"
      rm "$target"
    fi
  elif [[ -e "$target" ]]; then
    local backup="$${target}.bak.$$(date +%Y%m%d%H%M%S)"
    log_warn "Target exists and is not a symlink: $target"
    log_info "Backing up to: $backup"
    mv "$$target" "$$backup"
  fi

  # Создаём родительскую директорию если нужно
  mkdir -p "$$(dirname "$$target")"

  ln -s "$$source" "$$target"
  log_ok "Created symlink: $$target -> $$source"
}

# Очищает данные для конкретного NVIM_APPNAME
clean_data() {
  local appname="\$1"

  local dirs=(
    "$${NVIM_DATA_HOME}/$${appname}"
    "$${NVIM_STATE_HOME}/$${appname}"
    "$${NVIM_CACHE_HOME}/$${appname}"
  )

  for dir in "${dirs[@]}"; do
    if [[ -d "$dir" ]]; then
      log_info "Removing: $dir"
      rm -rf "$dir"
    fi
  done
}

# ─── Команда: install ───
# Устанавливает конфигурацию как изолированный NVIM_APPNAME
cmd_install() {
  local framework="\$1"
  local appname="$${2:-anvil-$${framework}}"

  if [[ ! -d "$${SRC_DIR}/$${framework}" ]]; then
    log_err "Framework '$${framework}' not found in $${SRC_DIR}/"
    log_info "Available: $$(ls -1 "$${SRC_DIR}" | grep -v common | tr '\n' ' ')"
    exit 1
  fi

  log_info "Installing '$${framework}' as NVIM_APPNAME='$${appname}'"

  local target="$${NVIM_CONFIG_HOME}/$${appname}"
  create_symlink "$${SRC_DIR}/$${framework}" "$target"

  # common подключается через runtimepath в bridge-файлах,
  # используя относительные пути от framework dir.
  if [[ ! -d "${SRC_DIR}/common" ]]; then
    log_warn "Common directory not found at ${SRC_DIR}/common"
    log_warn "Shared configuration will not be available"
  else
    log_ok "Common config available at ${SRC_DIR}/common"
  fi

  log_ok "Installation complete!"
  echo ""
  echo -e "  Run with: $${GREEN}NVIM_APPNAME=$${appname} nvim${NC}"
  echo ""
  echo -e "  Or add alias to your shell config:"
  echo -e "  $${CYAN}alias $${appname}='NVIM_APPNAME=$${appname} nvim'$${NC}"
}

# ─── Команда: install-default ───
# Устанавливает как основную конфигурацию nvim
cmd_install_default() {
  local framework="\$1"

  if [[ ! -d "$${SRC_DIR}/$${framework}" ]]; then
    log_err "Framework '${framework}' not found"
    exit 1
  fi

  log_info "Installing '${framework}' as default nvim config"

  local target="${NVIM_CONFIG_HOME}/nvim"
  create_symlink "$${SRC_DIR}/$${framework}" "$target"

  log_ok "Installed as default nvim configuration"
  echo ""
  echo -e "  Run with: $${GREEN}nvim$${NC}"
}

# ─── Команда: install-all ───
# Устанавливает все фреймворки как изолированные конфигурации
cmd_install_all() {
  log_info "Installing all frameworks as isolated configurations"

  for dir in "${SRC_DIR}"/*/; do
    local name
    name=$$(basename "$$dir")
    [[ "$name" == "common" ]] && continue
    [[ "$name" == "standalone" ]] && continue

    cmd_install "$$name" "anvil-$${name}"
    echo ""
  done

  log_ok "All frameworks installed!"
  echo ""
  echo "  Available configurations:"
  for dir in "${SRC_DIR}"/*/; do
    local name
    name=$$(basename "$$dir")
    [[ "$name" == "common" ]] && continue
    [[ "$name" == "standalone" ]] && continue
    echo -e "    $${GREEN}NVIM_APPNAME=anvil-$${name} nvim${NC}"
  done
}

# ─── Команда: uninstall ───
cmd_uninstall() {
  local appname="\$1"
  local target="$${NVIM_CONFIG_HOME}/$${appname}"

  if [[ -L "$target" ]]; then
    rm "$target"
    log_ok "Removed symlink: $target"
  elif [[ -e "$target" ]]; then
    log_warn "$target is not a symlink, refusing to remove"
    log_info "Remove manually if needed: rm -rf $target"
    return 1
  else
    log_warn "Nothing to remove: $target does not exist"
  fi

  read -rp "Clean data/state/cache for '${appname}'? [y/N] " answer
  if [[ "$$answer" =~ ^[Yy]$$ ]]; then
    clean_data "$appname"
    log_ok "Cleaned data for '${appname}'"
  fi
}

# ─── Команда: status ───
cmd_status() {
  echo -e "$${CYAN}Anvil Installation Status$${NC}"
  echo ""

  # Проверяем default nvim
  local nvim_config="${NVIM_CONFIG_HOME}/nvim"
  if [[ -L "$nvim_config" ]]; then
    local target
    target=$$(readlink -f "$$nvim_config" 2>/dev/null || readlink "$nvim_config")
    echo -e "  Default (nvim):  $${GREEN}→ $${target}${NC}"
  elif [[ -d "$nvim_config" ]]; then
    echo -e "  Default (nvim):  $${YELLOW}exists (not anvil symlink)$${NC}"
  else
    echo -e "  Default (nvim):  $${RED}not configured$${NC}"
  fi

  # Проверяем anvil-* конфигурации
  for dir in "${NVIM_CONFIG_HOME}"/anvil-*/; do
    [[ -d "$dir" ]] || continue
    local name
    name=$$(basename "$$dir")
    if [[ -L "${dir%/}" ]]; then
      local target
      target=$$(readlink -f "$${dir%/}" 2>/dev/null || readlink "${dir%/}")
      echo -e "  $${name}:  $${GREEN}→ $${target}$${NC}"
    else
      echo -e "  $${name}:  $${YELLOW}exists (not symlink)${NC}"
    fi
  done

  echo ""

  # Проверяем source
  echo -e "$${CYAN}Source Frameworks:$${NC}"
  for dir in "${SRC_DIR}"/*/; do
    local name
    name=$$(basename "$$dir")
    echo -e "  $${name}: $${GREEN}$${dir}$${NC}"
  done
}

# ─── Команда: bootstrap ───
# Запускает headless nvim для инициализации плагинов
cmd_bootstrap() {
  local appname="${1:-nvim}"

  log_info "Bootstrapping '${appname}'..."
  NVIM_APPNAME="$appname" nvim --headless -c 'quitall' 2>&1 || true
  log_ok "Bootstrap complete for '${appname}'"
}

# ─── Команда: shell-setup ───
# Выводит алиасы для добавления в shell config
cmd_shell_setup() {
  echo "# Anvil Neovim aliases"
  echo "# Add these to your ~/.bashrc, ~/.zshrc, or equivalent"
  echo ""

  for dir in "${SRC_DIR}"/*/; do
    local name
    name=$$(basename "$$dir")
    [[ "$name" == "common" ]] && continue

    echo "alias anvil-$${name}='NVIM_APPNAME=anvil-$${name} nvim'"
  done

  echo ""
  echo "# Function to switch between configurations"
  echo 'anvil() {'
  echo '  local config="${1:-astronvim}"'
  echo '  shift 2>/dev/null || true'
  echo '  NVIM_APPNAME="anvil-$${config}" nvim "$$@"'
  echo '}'
}

# ─── Usage ───
usage() {
  echo "Usage: \$0 <command> [args]"
  echo ""
  echo "Commands:"
  echo "  install <framework> [appname]  Install framework as NVIM_APPNAME"
  echo "  install-default <framework>    Install as default nvim config"
  echo "  install-all                    Install all frameworks"
  echo "  uninstall <appname>            Remove configuration"
  echo "  status                         Show installation status"
  echo "  bootstrap [appname]            Initialize plugins (headless)"
  echo "  shell-setup                    Print shell aliases"
  echo "  clean <appname>                Clean data/state/cache"
  echo "  help                           Show this help"
  echo ""
  echo "Frameworks: astronvim, nvchad, standalone"
  echo ""
  echo "Prerequisites: run ./preinstall.sh first to install system dependencies"
}

# ─── Main ───
main() {
  print_header
  check_deps

  local cmd="${1:-}"
  shift 2>/dev/null || true

  case "$cmd" in
    install)
      [[ $# -lt 1 ]] && { log_err "Framework name required"; usage; exit 1; }
      cmd_install "$@"
      ;;
    install-default)
      [[ $# -lt 1 ]] && { log_err "Framework name required"; usage; exit 1; }
      cmd_install_default "$@"
      ;;
    install-all)
      cmd_install_all
      ;;
    uninstall)
      [[ $# -lt 1 ]] && { log_err "App name required"; usage; exit 1; }
      cmd_uninstall "$@"
      ;;
    status)
      cmd_status
      ;;
    bootstrap)
      cmd_bootstrap "${1:-nvim}"
      ;;
    shell-setup)
      cmd_shell_setup
      ;;
    clean)
      [[ $# -lt 1 ]] && { log_err "App name required"; usage; exit 1; }
      clean_data "\$1"
      log_ok "Cleaned data for '\$1'"
      ;;
    help|--help|-h|"")
      usage
      ;;
    *)
      log_err "Unknown command: $cmd"
      usage
      exit 1
      ;;
  esac
}

main "$@"
```

### Makefile

```makefile
.PHONY: help install-astro install-nvchad install-all \
        uninstall-astro uninstall-nvchad status bootstrap-all \
        shell-setup clean-all preinstall

SHELL := /bin/bash

help: ## Show this help
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$$' $$(MAKEFILE_LIST) | \
		awk 'BEGIN {FS = ":.*?## "}; {printf "  \033[36m%-20s\033[0m %s\n", $$1, $$2}'

preinstall: ## Install system dependencies (neovim, fonts, tools)
	@./preinstall.sh

install-astro: ## Install AstroNvim config (as anvil-astronvim)
	@./install.sh install astronvim

install-nvchad: ## Install NvChad config (as anvil-nvchad)
	@./install.sh install nvchad

install-all: ## Install all framework configs
	@./install.sh install-all

install-default-astro: ## Install AstroNvim as default nvim config
	@./install.sh install-default astronvim

install-default-nvchad: ## Install NvChad as default nvim config
	@./install.sh install-default nvchad

uninstall-astro: ## Uninstall AstroNvim config
	@./install.sh uninstall anvil-astronvim

uninstall-nvchad: ## Uninstall NvChad config
	@./install.sh uninstall anvil-nvchad

status: ## Show installation status
	@./install.sh status

bootstrap-all: install-all ## Install all and bootstrap plugins
	@./install.sh bootstrap anvil-astronvim
	@./install.sh bootstrap anvil-nvchad

shell-setup: ## Print shell aliases
	@./install.sh shell-setup

clean-all: ## Clean all anvil data/state/cache
	@./install.sh clean anvil-astronvim
	@./install.sh clean anvil-nvchad
```

---

## Как это работает

### Механизм подключения common

```
┌─────────────────────────────────────────────────────┐
│                   NVIM_APPNAME                      │
│          (anvil-astronvim или anvil-nvchad)         │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│  src/astronvim/init.lua или src/nvchad/init.lua     │
│  (оригинальный bootstrap — БЕЗ ИЗМЕНЕНИЙ для       │
│   astronvim, минимальные для nvchad)                │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│  anvil_bridge.lua                                    │
│  1. Вычисляет путь к src/common через debug.getinfo │
│  2. Добавляет src/common в vim.opt.runtimepath      │
│  3. Вызывает require("anvil").setup()               │
│  4. Возвращает common plugin specs                  │
└──────────┬──────────────────────┬────────────────────┘
           │                      │
           ▼                      ▼
┌─────────────────┐  ┌────────────────────┐
│  src/common/    │  │ Framework-specific  │
│  lua/anvil/     │  │ plugins & config    │
│  - options      │  │ (astrocore, etc.)   │
│  - keymaps      │  │                     │
│  - autocmds     │  │                     │
│  - plugins/     │  │                     │
└─────────────────┘  └────────────────────┘
```

### Почему симлинки, а не копирование

Симлинк `~/.config/anvil-astronvim → repo/src/astronvim` означает:

- **Относительные пути в bridge работают** — `debug.getinfo` возвращает
  реальный путь через симлинк, и `../common` разрешается корректно
- **Изменения в репозитории сразу видны** — не нужно пересинхронизировать
- **Git отслеживает всё в одном месте**

### Изоляция через NVIM_APPNAME

Neovim 0.10+ поддерживает `NVIM_APPNAME`, что даёт полную изоляцию:

```
NVIM_APPNAME=anvil-astronvim
  config: ~/.config/anvil-astronvim/
  data:   ~/.local/share/anvil-astronvim/
  state:  ~/.local/state/anvil-astronvim/
  cache:  ~/.cache/anvil-astronvim/

NVIM_APPNAME=anvil-nvchad
  config: ~/.config/anvil-nvchad/
  data:   ~/.local/share/anvil-nvchad/
  state:  ~/.local/state/anvil-nvchad/
  cache:  ~/.cache/anvil-nvchad/

nvim (default, без NVIM_APPNAME)
  config: ~/.config/nvim/
  data:   ~/.local/share/nvim/
  ...
```

Все три конфигурации работают полностью независимо — свои плагины, свой кэш,
своё состояние.

---

## Типичные сценарии использования

### Установить оба фреймворка рядом со штатным nvim

```bash
# Устанавливаем системные зависимости (если ещё не установлены)
./preinstall.sh

# Клонируем репозиторий
git clone https://github.com/username/anvil ~/.local/src/anvil
cd ~/.local/src/anvil

# Устанавливаем оба
make install-all

# Инициализируем плагины
make bootstrap-all

# Добавляем алиасы в shell
make shell-setup >> ~/.bashrc
source ~/.bashrc

# Теперь доступны три конфигурации:
nvim               # штатная конфигурация (не затронута)
anvil-astronvim    # AstroNvim + anvil common
anvil-nvchad       # NvChad + anvil common
```

### Установить только AstroNvim как основной редактор

```bash
cd ~/.local/src/anvil
./install.sh install-default astronvim
./install.sh bootstrap nvim

# Теперь nvim запускается с AstroNvim + anvil
nvim
```

### Установить только NvChad изолированно

```bash
cd ~/.local/src/anvil
./install.sh install nvchad

NVIM_APPNAME=anvil-nvchad nvim
```

### Расширение общей конфигурации

Чтобы добавить новый общий плагин, достаточно создать файл в
`src/common/lua/anvil/plugins/` или дополнить существующий:

```lua
-- src/common/lua/anvil/plugins/editor.lua
-- Добавляем новый плагин в массив:
return {
  -- ... существующие плагины ...

  {
    "folke/flash.nvim",
    event = "VeryLazy",
    opts = {},
    keys = {
      { "s", mode = { "n", "x", "o" },
        function() require("flash").jump() end,
        desc = "Flash" },
    },
  },
}
```

Этот плагин автоматически появится в обоих фреймворках при следующем запуске,
потому что `anvil_bridge.lua` вызывает `anvil.plugin_specs()`, который собирает
все модули из `anvil.plugins.*`.

---

## Файлы, которые НЕ изменяются (остаются из оригинальных templates)

### AstroNvim (сохраняются как есть из starter template)

- `src/astronvim/init.lua` — оригинальный bootstrap
- `src/astronvim/lua/lazy_setup.lua` — оригинальный (с community import)
- `src/astronvim/lua/community.lua` — оригинальный (с guard)
- `src/astronvim/lua/polish.lua` — оригинальный (с guard)
- `src/astronvim/lua/plugins/astrolsp.lua` — оригинальный (с guard)
- `src/astronvim/lua/plugins/astroui.lua` — оригинальный (с guard)
- `src/astronvim/lua/plugins/none-ls.lua` — оригинальный (с guard)
- `src/astronvim/lua/plugins/user.lua` — оригинальный (с guard)
- `src/astronvim/selene.toml` — оригинальный
- `src/astronvim/neovim.yml` — оригинальный

### NvChad (сохраняются как есть из starter template)

- `src/nvchad/lua/chadrc.lua` — оригинальный
- `src/nvchad/lua/autocmds.lua` — оригинальный
- `src/nvchad/lua/configs/conform.lua` — оригинальный
- `src/nvchad/lua/configs/lazy.lua` — оригинальный
- `src/nvchad/lua/configs/lspconfig.lua` — оригинальный

### Общие

- `preinstall.sh` — скрипт установки зависимостей
